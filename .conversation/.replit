run = "uvicorn main:app --host 0.0.0.0 --port 8000"
entrypoint = "main.py"
modules = ["python-3.11"]

[nix]
channel = "stable-23_11"
packages = ["libxcrypt"]

[deployment]
run = ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]

[[ports]]
localPort = 8000
externalPort = 80
